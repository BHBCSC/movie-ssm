<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csc.movie.dao.CelebrityDAO">
    <resultMap id="ActorResultMap" type="com.csc.movie.entity.Celebrity">
        <id column="actor_id" property="id" />
        <result column="name" property="name" />
        <collection property="movieList" ofType="com.csc.movie.entity.Movie" column="actor_id">
            <id column="movie_id" property="id"/>
            <result column="name" property="name"/>
            <result column="score" property="score"/>
        </collection>
    </resultMap>
    
    <!-- 目的：为dao接口方法提供sql语句配置 -->
    <select id="queryById" resultType="Actor" parameterType="int">
        <!-- 具体的sql -->
        SELECT
            id,
            name
        FROM
            actor
        WHERE
            id = #{id}
    </select>

    <select id="queryByMovie" resultMap = "ActorResultMap" parameterType="int">
        <!-- 具体的sql -->
        SELECT
        a.*, m.*
        FROM
        actor a,movie m
        WHERE
        a.actor_id = m.actor_id AND a.actor_id = #{id}
    </select>

    <select id="queryAll" resultType="Actor">
        SELECT
            id,
            name
        FROM
            actor
        ORDER BY
            id
        LIMIT #{offset}, #{limit}
    </select>



    <select id="count" resultType="int">
        SELECT
        count(*)
        FROM
        movie
    </select>

    <!--<update id="reduceNumber">
        UPDATE book
        SET number = number - 1
        WHERE
            book_id = #{bookId}
        AND number > 0
    </update>-->
</mapper>